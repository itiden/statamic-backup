providers: []

migrations:
  - workbench/database/migrations

seeders: []

bootstrappers:
 - 'Itiden\Backup\Tests\Testbench\Bootstrappers\ComposerBootstrapper'

workbench:
  start: '/cp'
  install: true
  welcome: true
  build: [
    'migrate:fresh'
  ]
  assets: [
    'statamic'
  ]
  sync: []

env:
 DB_CONNECTION: sqlite
 APP_ENV: testing
 CACHE_STORE: array
 QUEUE_CONNECTION: sync